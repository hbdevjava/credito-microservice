spring: 
  application:
    name: ms-clientes
    
server:
  port: 0
  
      
eureka:
  client:
    service-url:
      defaultZone: http://cursoms-eureka-user:12111660@localhost:8761/eureka

  instance:
    instance-id: ${spring.application.name}:${spring.application.instance-id:${random.value}}
    
  spring:
    datasource:
      url: jdbc:h2:mem
      username: sa
      password: 12345
      driverClassName: org.h2.Driver
      
      
    spring:
      h2:
       console:
        enabled: true
